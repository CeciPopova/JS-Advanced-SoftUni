function solve(first, second, third) {
    let firstLength = first.length;
    let secondLengtgh = second.length;
    let thirdLendth = third.length;
    console.log(firstLength + secondLengtgh + thirdLendth);
    let average = Math.floor((firstLength + secondLengtgh + thirdLendth)/3);

    console.log(average);